# Dependencies never run install scripts. No package in the tree needs one today, so a future
# package that adds one fails loudly in CI instead of running code on every developer's machine.
ignore-scripts=true
